# Corrected Top-Level Makefile.am
SUBDIRS = src

# FIX: Define the directory variable (nm_vpn_plugindir) correctly before use.
nm_vpn_plugindir = $(nmvpn_plugin_dir)
nm_vpn_plugin_DATA = nm-nebulavpn.name

EXTRA_DIST = \
    nm-nebulavpn.name
